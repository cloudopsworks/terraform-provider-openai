# OpenAI provider

This provider uses the OpenAI Administration APIs to manage organization projects,
project service accounts, and service-account project API keys. It uses an
existing admin API key and can resolve that key from AWS Secrets Manager, GCP
Secret Manager, or Azure Key Vault.
